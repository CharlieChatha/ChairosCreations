{
  "name": "chairo-s-creations",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
     "deploy": "vite build && git add . && git commit -m 'Deploy to GitHub Pages' && git push origin gh-pages"
  },
  
  "devDependencies": {
    "vite": "^5.4.1"
  }
}

